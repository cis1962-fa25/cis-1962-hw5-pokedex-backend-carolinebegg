# Port your Express server will run on
PORT=3000

# Secret used to sign JWT tokens.
# Generate your own random string and put it in .env
JWT_TOKEN_SECRET=REPLACE_ME_WITH_A_SECURE_RANDOM_STRING

# Redis connection string.
# For local Redis use:
REDIS_URL=redis://localhost:6379